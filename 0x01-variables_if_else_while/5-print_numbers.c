#include <stdio.h>

/**
 * main - Prints only single digit numbers
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int i = 0;
for ( ; i < 10 ; i++)
{
printf("%d",i);
}
printf("\n");
return (0);
}
