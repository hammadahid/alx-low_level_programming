#include <stdio.h>

/**
 * main - main block
 * Description: Get a random number and check its last digit, compare it with 5
 * Return: 0
 */
char a = 97;
int main(void)
{
for ( ; a <= 122 ; a++)
{
putchar(a);
}
putchar('\n');
return (0);
}
