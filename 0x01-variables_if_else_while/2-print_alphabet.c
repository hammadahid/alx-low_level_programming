#include <stdio.h>

/**
 * main - main block
 * Description: Get a random number and check its last digit, compare it with 5
 * Return: 0
 */

int main(void)
{
char a = 97;
for ( ; a <= 122 ; a++)
{
putchar(a);
}
putchar('\n');
return (0);
}
