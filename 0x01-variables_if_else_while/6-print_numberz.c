#include <stdio.h>

/**
 * main - Prints only single digit numbers
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int i = 48;
for ( ; i < 58 ; i++)
{
putchar(i);
}
putchar('\n');
return (0);
}
