#include <stdio.h>

/**
 * main - Prints only single digit numbers
 *
 * Return: Always 0 (Success)
 */
int i = 48;
int main(void)
{
    for (; i < 58 ; i++)
    {
        putchar(i);
    }
    putchar('\n');
    return (0);
}
