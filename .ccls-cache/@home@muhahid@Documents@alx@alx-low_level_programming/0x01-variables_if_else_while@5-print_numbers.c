#include <stdio.h>

/**
 * main - Prints only single digit numbers
 *
 * Return: Always 0 (Success)
 */
int i = 0;
int main(void)
{
    for (; i < 10 ; i++)
    {
        printf("%d",i);
    }
    printf("\n");
    return (0);
}
