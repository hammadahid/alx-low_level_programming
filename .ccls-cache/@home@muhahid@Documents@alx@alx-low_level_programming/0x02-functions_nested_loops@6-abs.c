#include "main.h"

/**
 * main: _abs function
 * Description: return absolute value of an integer
 * Return: absolute value of an int
 */

int _abs(int n)
{
if ( n < 1 )
{
    return (n *(-1));
}
return n;
}
